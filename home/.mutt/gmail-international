unmailboxes *
set folder      = imaps://imap.gmail.com/
set imap_user   = international@uaar.it
set imap_pass   = `secret-tool lookup user international domain uaar.it`
set spoolfile   = +INBOX
mailboxes       = +INBOX
set imap_list_subscribed
set imap_check_subscribed

set header_cache = ~/.cache/mutt/gmail.international/headers
set message_cachedir = "~/.cache/mutt/gmail.international/bodies"

set from = "international@uaar.it"                            
set realname = "Massimo Redaelli"

set postponed = +[Gmail]/Drafts
set record = "+[Gmail]/Sent Mail"

