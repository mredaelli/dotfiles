source ~/.config/neomutt/unbind
source ~/.config/neomutt/bindings

# Folder hooks
folder-hook 'main' 'source ~/.config/neomutt/mredaelli'
folder-hook 'fast' 'source ~/.config/neomutt/fast'

source ~/.config/neomutt/fast

source ~/.config/neomutt/dracula.muttrc

set mailcap_path= ~/.config/neomutt/mailcap

set quit=ask-yes                    
set beep=no
set beep_new=yes      
set sleep_time = 0
unset wait_key

set fast_reply=yes
set include=yes

set query_command= "khard email --parsable '%s'"

# no confirm on send?
#set fast_reply=yes
#set include=yes

# Pager View Options ---------------------------------
set pager_index_lines = 10 # number of index lines to show
set pager_context = 3      # number of context lines to show
set pager_stop             # don't go to next message automatically
set menu_scroll            # scroll in menus
set tilde                  # show tildes like in vim
unset markers              # no ugly plus signs

set quote_regexp = "^( {0,4}[>|:#%]| {0,4}[a-z0-9]+[>|]+)+"
set forward_format="Fw: %s"

set sidebar_short_path
set sidebar_delim_chars="/"
set sidebar_folder_indent
set sidebar_indent_string=" "
set sidebar_visible
set sidebar_format="%B%?F? [%F]?%* %?N?%N/?%S" 


alternative_order text/enriched text/plain text/html
auto_view text/html application/text application/pdf

# Index View Options ---------------------------------
set date_format = "%d.%m.%y %H:%M"
set index_format = "%5C [%Z%E] %D %-20.20F  %s"
set sort = threads                         # like gmail
set sort_aux = reverse-last-date-received  # like gmail
set uncollapse_jump                        # don't collapse on an unread message
set sort_re                                # thread based on regex
set reply_regexp = "^(([Rr][Ee]?(\[[0-9]+\])?: *)?(\[[^]]+\] *)?)*"

set mail_check_stats

set text_flowed
